@model Restaurante.ViewModels.Productos.NuevoProductoViewModel

@{
    ViewBag.Title = "Editar";
}

    <div class="container">
        <h4 class="text-center mt-5">
            Editar Producto: @Model.Descripcion
        </h4>

        <div class="row">
            <div class="offset-3 col-6">
                @using (Html.BeginForm("Editar", "Productos", FormMethod.Post))
                {
                    <div class="form-group">
                        <label for="Descripción">Descripcion</label>
                        @Html.TextBoxFor(x => x.Descripcion, new { @class = "form-control", placeholder = @Model.Descripcion })
                        @Html.ValidationMessageFor(x => x.Descripcion, "", new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        <label for="Presio">Precio</label>
                        @Html.TextBoxFor(x => x.Precio, new { @class = "form-control", placeholder = @Model.Precio, @type = "numeric" })
                        @Html.ValidationMessageFor(x => x.Precio, "", new { @class = "text-danger" })
                    </div>
                    @Html.ValidationMessage("", new { @class = "text-danger" })
                    <button type="submit" class="btn btn-dark">
                        Confirmar
                    </button>
                    <a class="btn btn-outline-dark"
                       href="@Url.Action("Index","Productos")">
                        Cancelar
                    </a>
                }
            </div>
        </div>
    </div>

